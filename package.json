{
  "name": "@talismn/chaindata",
  "version": "0.0.1",
  "type": "module",
  "description": "A community controlled index of relay/parachain assets to suppliment on-chain data, allowing developers to easily leverage the latest parachain information when building within the polkadot ecosystem.",
  "repository": "https://github.com/TalismanSociety/chaindata.git",
  "author": "Talisman Devs <devs@talisman.community>",
  "keywords": [],
  "license": "GPL-3.0-only",
  "bugs": {
    "url": "https://github.com/TalismanSociety/chaindata/issues"
  },
  "homepage": "https://talisman.community",
  "scripts": {
    "validate-json": "bun run prettier --write *.json",
    "validate-schema-chains": "bun run ajv --spec=draft2020 -s schemas/json/chaindata.json -d chaindata.json -d testnets-chaindata.json",
    "validate-schema-evm-networks": "bun run ajv --spec=draft2020 -s schemas/json/evm-networks.json -d evm-networks.json",
    "validate-schema-tokens": "bun run ajv --spec=draft2020 -s schemas/json/tokens.json -d tokens.json",
    "validate-schema-tokens-buyable": "bun run ajv --spec=draft2020 -s schemas/json/ids.json -d tokens-buyable.json",
    "validate-schema": "bun run validate-schema-chains && bun run validate-schema-evm-networks && bun run validate-schema-tokens && bun run validate-schema-tokens-buyable",
    "validate": "bun run validate-json && bun run validate-schema",
    "update-pjs": "bun update",
    "import-pjs-config": "bun run scripts/import-pjs-config.js",
    "update": "bun run update-pjs && bun run import-pjs-config",
    "build": "bun run scripts/build.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@polkadot/apps-config": "*",
    "@polkadot/networks": "*",
    "ajv-cli": "^5.0.0",
    "lodash": "^4.17.21",
    "prettier": "^2.7.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
